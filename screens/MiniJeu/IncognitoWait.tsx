import React from "react";
import { View, Text, StyleSheet } from "react-native";


const IncognitoWait = () => {

    return (
        <View style={styles.global}>
            <Text>Page IncognitoWait</Text>
        </View>
    );
};

const styles = StyleSheet.create({
    global: {
        flex:1
    },
});

export default IncognitoWait;
